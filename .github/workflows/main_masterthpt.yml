name: Build and deploy to Azure Container Apps

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}
  IMAGE_TAG: ${{ github.sha }}
  AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
  AZURE_LOCATION: southeastasia
  CONTAINERAPPS_ENV_NAME: masterthpt-env
  API_APP_NAME: masterthpt-api
  WEB_APP_NAME: masterthpt-web

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ACR_LOGIN_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push API image
        uses: docker/build-push-action@v5
        with:
          context: ./master/apps/api
          file: ./master/apps/api/Dockerfile
          push: true
          tags: |
            ${{ secrets.ACR_LOGIN_SERVER }}/masterthpt-api:${{ github.sha }}
            ${{ secrets.ACR_LOGIN_SERVER }}/masterthpt-api:latest
          cache-from: type=gha,scope=api
          cache-to: type=gha,mode=max,scope=api

      - name: Build and push Web image
        uses: docker/build-push-action@v5
        with:
          context: ./master/apps/web
          file: ./master/apps/web/Dockerfile
          push: true
          tags: |
            ${{ secrets.ACR_LOGIN_SERVER }}/masterthpt-web:${{ github.sha }}
            ${{ secrets.ACR_LOGIN_SERVER }}/masterthpt-web:latest
          build-args: |
            NEXT_PUBLIC_GOOGLE_CLIENT_ID=${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}
            NEXT_PUBLIC_API_URL=/api
            API_PROXY_TARGET=http://${{ env.API_APP_NAME }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,mode=max,scope=web


  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Ensure Azure Container Apps extension
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            az extension add --name containerapp --upgrade

      - name: Ensure Container Apps environment exists
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            set -euo pipefail
            if ! az containerapp env show \
              --name "${{ env.CONTAINERAPPS_ENV_NAME }}" \
              --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" >/dev/null 2>&1; then
              az containerapp env create \
                --name "${{ env.CONTAINERAPPS_ENV_NAME }}" \
                --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
                --location "${{ env.AZURE_LOCATION }}"
            fi

      - name: Create or update API container app
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            set -euo pipefail

            GOOGLE_CLIENT_ID_VALUE="${{ secrets.GOOGLE_CLIENT_ID }}"
            if [ -z "${GOOGLE_CLIENT_ID_VALUE}" ]; then
              GOOGLE_CLIENT_ID_VALUE="${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}"
            fi

            if az containerapp show \
              --name "${{ env.API_APP_NAME }}" \
              --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" >/dev/null 2>&1; then
              az containerapp secret set \
                --name "${{ env.API_APP_NAME }}" \
                --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
                --secrets \
                  database-url="${{ secrets.DATABASE_URL }}" \
                  jwt-secret="${{ secrets.JWT_SECRET }}"

              az containerapp registry set \
                --name "${{ env.API_APP_NAME }}" \
                --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
                --server "${{ secrets.ACR_LOGIN_SERVER }}" \
                --username "${{ secrets.ACR_USERNAME }}" \
                --password "${{ secrets.ACR_PASSWORD }}"

              az containerapp update \
                --name "${{ env.API_APP_NAME }}" \
                --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
                --image "${{ secrets.ACR_LOGIN_SERVER }}/masterthpt-api:${{ github.sha }}" \
                --set-env-vars \
                  PORT=3001 \
                  DATABASE_URL=secretref:database-url \
                  JWT_SECRET=secretref:jwt-secret \
                  JWT_EXPIRES_IN=30d \
                  GOOGLE_CLIENT_ID="${GOOGLE_CLIENT_ID_VALUE}" \
                  AI_API_BASE_URL=https://mkp-api.fptcloud.com \
                  USE_MOCK_SERVICES=true
            else
              az containerapp create \
                --name "${{ env.API_APP_NAME }}" \
                --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
                --environment "${{ env.CONTAINERAPPS_ENV_NAME }}" \
                --image "${{ secrets.ACR_LOGIN_SERVER }}/masterthpt-api:${{ github.sha }}" \
                --registry-server "${{ secrets.ACR_LOGIN_SERVER }}" \
                --registry-username "${{ secrets.ACR_USERNAME }}" \
                --registry-password "${{ secrets.ACR_PASSWORD }}" \
                --ingress internal \
                --target-port 3001 \
                --min-replicas 1 \
                --max-replicas 3 \
                --cpu 2 \
                --memory 4.0Gi \
                --secrets \
                  database-url="${{ secrets.DATABASE_URL }}" \
                  jwt-secret="${{ secrets.JWT_SECRET }}" \
                --env-vars \
                  PORT=3001 \
                  DATABASE_URL=secretref:database-url \
                  JWT_SECRET=secretref:jwt-secret \
                  JWT_EXPIRES_IN=30d \
                  GOOGLE_CLIENT_ID="${GOOGLE_CLIENT_ID_VALUE}" \
                  AI_API_BASE_URL=https://mkp-api.fptcloud.com \
                  USE_MOCK_SERVICES=true
            fi

      - name: Create or update Web container app
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            set -euo pipefail

            if az containerapp show \
              --name "${{ env.WEB_APP_NAME }}" \
              --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" >/dev/null 2>&1; then
              az containerapp registry set \
                --name "${{ env.WEB_APP_NAME }}" \
                --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
                --server "${{ secrets.ACR_LOGIN_SERVER }}" \
                --username "${{ secrets.ACR_USERNAME }}" \
                --password "${{ secrets.ACR_PASSWORD }}"

              az containerapp update \
                --name "${{ env.WEB_APP_NAME }}" \
                --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
                --image "${{ secrets.ACR_LOGIN_SERVER }}/masterthpt-web:${{ github.sha }}" \
                --set-env-vars \
                  PORT=3000 \
                  NEXT_PUBLIC_API_URL=/api \
                  API_PROXY_TARGET="http://${{ env.API_APP_NAME }}" \
                  NEXT_PUBLIC_GOOGLE_CLIENT_ID="${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}"
            else
              az containerapp create \
                --name "${{ env.WEB_APP_NAME }}" \
                --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
                --environment "${{ env.CONTAINERAPPS_ENV_NAME }}" \
                --image "${{ secrets.ACR_LOGIN_SERVER }}/masterthpt-web:${{ github.sha }}" \
                --registry-server "${{ secrets.ACR_LOGIN_SERVER }}" \
                --registry-username "${{ secrets.ACR_USERNAME }}" \
                --registry-password "${{ secrets.ACR_PASSWORD }}" \
                --ingress external \
                --target-port 3000 \
                --min-replicas 1 \
                --max-replicas 3 \
                --cpu 0.5 \
                --memory 1.0Gi \
                --env-vars \
                  PORT=3000 \
                  NEXT_PUBLIC_API_URL=/api \
                  API_PROXY_TARGET="http://${{ env.API_APP_NAME }}" \
                  NEXT_PUBLIC_GOOGLE_CLIENT_ID="${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}"
            fi

      - name: Print web URL
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            WEB_FQDN=$(az containerapp show \
              --name "${{ env.WEB_APP_NAME }}" \
              --resource-group "${{ env.AZURE_RESOURCE_GROUP }}" \
              --query "properties.configuration.ingress.fqdn" -o tsv)
            echo "Web URL: https://${WEB_FQDN}"
