services:
  ai:
    build:
      context: ../master/agents
      dockerfile: Dockerfile
    container_name: masterthpt-ai
    restart: unless-stopped
    env_file:
      - ./.env.ai
    expose:
      - "8001"